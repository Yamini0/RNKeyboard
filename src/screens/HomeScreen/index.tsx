import React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const HomeScreen = () => {
  return (
    <SafeAreaView edges={['top']}>
      <Text>Radha soami</Text>
    </SafeAreaView>
  );
};
export default HomeScreen;
